# =============================================================================
# Python 依赖包配置文件
# 用于 NCCL 测试容器的 Python 环境
# 基于 nccl_python_template.py 的实际需求
# =============================================================================

# 核心依赖 - NCCL 测试必需
# 注意：torch 通常通过 NVIDIA PyTorch 容器镜像提供，无需单独安装
# 如果需要单独安装，请取消下面的注释并指定版本
# torch>=2.1.0

# 系统监控和进程管理 - 用于监控测试过程中的系统资源
psutil==5.9.8

# 数值计算库 - PyTorch 的基础依赖，用于数值计算
numpy==1.24.4

# 数据可视化 - 用于测试结果分析和性能图表生成
matplotlib==3.7.5
seaborn==0.13.2

# 网络和通信工具 (可选) - 用于网络诊断和 HTTP 请求
# requests==2.31.0
# urllib3==2.0.7

# 日志和调试工具 (可选) - 增强日志输出和调试体验
# loguru==0.7.2
# rich==13.7.0

# 性能分析工具 (可选) - 用于深度性能分析
# py-spy==0.3.14
# memory-profiler==0.61.0

# 配置文件处理 (可选) - 用于配置文件解析
# pyyaml==6.0.1
# toml==0.10.2

# 分布式训练相关 (可选) - 如果需要额外的分布式工具
# mpi4py==3.1.5  # 需要系统安装 MPI

# =============================================================================
# 说明：
# 1. torch 和 torch.distributed 是 NCCL 测试的核心依赖
# 2. 在 NVIDIA PyTorch 容器中，PyTorch 已预装，无需在此指定
# 3. 如果在非容器环境中运行，需要手动安装 PyTorch
# 4. 标准库模块 (os, sys, time, signal, socket) 无需在此列出
# =============================================================================